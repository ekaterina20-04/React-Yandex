# 🌌 Сервис межгалактической аналитики — Интерфейс

Это React-приложение является клиентской частью Сервиса межгалактической аналитики. Интерфейс позволяет загружать таблицы для аналитики, генерировать тестовые данные, просматривать историю загрузок и отслеживать прогресс обработки данных.

Проект реализован по утвержденному дизайну и функциональным требованиям.

## Как запустить

1. Клонируйте репозиторий:

   ```bash
   git clone git@github.com:ekaterina20-04/React-Yandex.git
   cd <ИМЯ_ПАПКИ>
   ```

2. Установите зависимости:

   ```bash
   npm install
   ```

3. Запустите backend-сервер (порт `3000`):

   ```bash
   npm run start
   ```

4. Запустите клиентское приложение (порт `5173`):
   ```bash
   npm run dev
   ```

> ⚠️ Приложение разрабатывалось и тестировалось в браузере Google Chrome с шириной окна 1920px. Для корректного отображения верстки рекомендуется использовать аналогичные параметры.

## Архитектура проекта

```
/public/img             # Все изображения проекта
/src
├── app/                # Точка входа, роутинг, глобальные стили
│   ├── layout/         # Общие макеты
├── components/
│   ├── header/         # Компонент шапки
│   └── ui/             # Универсальные переиспользуемые компоненты (кнопки и т.п.)
├── entities/
│   ├── history/        # Логика хранения и отображения истории (LocalStorage)
│   ├── analytics/      # Обработка загружаемых таблиц
│   └── generation/     # Генерация тестовых таблиц
└── pages/
    ├── history/        # Страница истории загрузок
    ├── analytics/      # Главная страница (загрузка и отображение аналитики)
    └── generation/     # Страница генерации данных
```

## Технические особенности

- **Сборка:** Vite
- **Язык:** TypeScript
- **Стили:** CSS Modules
- **Роутинг:** `react-router-dom`
- **State management:** Zustand
- **Запросы:** Fetch API
- **Модальные окна:** Реализованы через React Portals
- **История:** Хранение в LocalStorage
- **Кодстайл:** ESLint + Prettier
- **Библиотеки:** Только утвержденные, сторонние библиотеки не использовались

## 📋 Реализованный функционал

- Загрузка таблиц (drag&drop или кнопка)
- Прогрессивная отправка и отображение данных
- Обработка ошибок при запросах
- Индикатор загрузки
- Сброс состояния ("Очистить")
- Навигационное меню и роутинг
- Генерация тестовых таблиц
- История загрузок с возможностью:
  - Просмотра в модальном окне
  - Удаления отдельной записи
  - Очистки всей истории
  - Перехода к генерации из истории
